# TaskFlow (taskflow-app)

Minimal README for local development and quick start.

Prerequisites
- Java 21 installed and `JAVA_HOME` configured (or use the JDK on your system).
- Maven installed (`mvn` on PATH) or use an IDE that runs Maven builds.

Build
```bash
cd /mnt/c/Users/garre/Documents/Epitech/code/TaskFlow/taskflow-app
mvn -DskipTests clean package
```

Run (development)
```bash
# run with Maven
mvn -DskipTests spring-boot:run
```

Run on a different port
```bash
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=8081"
```

si le port est deja occuper 
```bash
sudo netstat -tulpn | grep :8080
sudo kill -9 <PID>
```

Credentials (development)
- The project contains a development account configured in `src/main/resources/application.properties`.
- Default dev credentials (only for local development): `admin` / `admin`.

Notes
- `target/` is generated by Maven and is ignored by Git (see `.gitignore`).
- For learning, keep tests under `src/test/java` and add simple JUnit tests to practice.
- Do not commit production secrets — this README assumes a local dev setup.

Where to find the main class
- The Spring Boot entry point is `com.taskflow.Application` in `src/main/java/com/taskflow/Application.java`.

If you want I can add a simple example controller or a CI workflow next.
